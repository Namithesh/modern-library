<%- include('partials/header.ejs') %>

    <!-- Line --><div class="main-content" style="margin-top:35px;padding-left:20px; padding-right:20px; padding-bottom:25px;">

    <div class="fluid-container p-4">
    <h1 class="display-3">Gallery</h1>

    <% if (role === 'ADMIN') { %>
        <a style="margin-bottom: 25px;" class="btn btn-outline-dark" href="/gallery/add">Add New Image</a>
    <% } %>

    <!-- START -->
        <div class="row">

            <% for (let i=0; i<data.length; i++) { %>
                <div class="col-md-3 cover">
                    <a href="/<%=data[i].image_url%>" target="_blank"><img src="/<%=data[i].image_url%>" width="100%" height="300px"/></a>    
                    <p><%=data[i].upload_date%></p>
                </div>
            <% } %>

        </div>
    <!-- END -->
   
    </div>
    </div>

<%- include('partials/footer.ejs') %>
